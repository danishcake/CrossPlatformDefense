#include "Block.h"
namespace BlockType
{
    /*
     * Defines the default strength of each block type. Must correspond to BlockType::Enum
     */
    unsigned char blockHealth[] =
    {
        0,
        32,
        32,
        64,
        96,
        128,
        196
    };
}
