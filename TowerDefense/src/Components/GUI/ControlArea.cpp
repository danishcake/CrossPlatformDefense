#include "ControlArea.h"
#include "../../Camera.h"

ControlArea::ControlArea()
{
}

ControlArea::ControlArea(UDim position, UDim size)
   : mPosition(position), mSize(size)
{
}

void ControlArea::Tick(TickParameters& tp)
{

}